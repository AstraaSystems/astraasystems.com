[build]
  publish = "."

# Astraa API proxy will be enabled after the production backend is deployed.
# Final format:
#
# [[redirects]]
#   from = "/api/*"
#   to = "https://YOUR-REAL-BACKEND-DOMAIN/api/:splat"
#   status = 200
#   force = true
